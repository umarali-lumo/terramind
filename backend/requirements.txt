# TerraMind — Vercel serverless deployment dependencies.
#
# Same as requirements-render.txt minus uvicorn (the Vercel Python runtime
# drives the ASGI app directly) and minus the PyTorch / transformers AI
# stack, which cannot ship in a serverless bundle. Every other service
# (auth, farms, weather, health, irrigation, yield, alerts, copilot, IoT)
# runs unchanged, and the disease API degrades gracefully to a clear 503
# "model_unavailable" response.
#
# Local / full installs (with the disease model) use the repository-root
# requirements.txt.

fastapi>=0.110
python-multipart>=0.0.9

sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.2

PyJWT>=2.8

httpx>=0.27

pillow>=10.3
